name: Pyasic Plugin Checks

on:
  pull_request:
    paths:
      - ".github/workflows/protofleet-pyasic-plugin-checks.yml"
      - ".github/actions/hermit-setup/action.yml"
      - "bin/hermit.hcl"
      - "hermit-packages/**"
      - "plugin/pyasic/**"
      - "server/sdk/v1/python/**"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/protofleet-pyasic-plugin-checks.yml"
      - ".github/actions/hermit-setup/action.yml"
      - "bin/hermit.hcl"
      - "hermit-packages/**"
      - "plugin/pyasic/**"
      - "server/sdk/v1/python/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pyasic-plugin:
    name: pyasic-plugin
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: plugin/pyasic
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Hermit
        uses: ./.github/actions/hermit-setup

      - name: Setup venv and install dependencies
        run: |
          python3 -m venv .venv
          .venv/bin/pip install --upgrade pip setuptools
          .venv/bin/pip install -e "../../server/sdk/v1/python"
          .venv/bin/pip install -e ".[dev]"

      - name: Run tests
        run: .venv/bin/pytest tests/ -v --cov=pyasic_driver --cov-report=term-missing

      - name: Lint
        run: .venv/bin/ruff check pyasic_driver/ tests/

      - name: Typecheck
        run: |
          .venv/bin/pip install types-PyYAML
          .venv/bin/mypy pyasic_driver/

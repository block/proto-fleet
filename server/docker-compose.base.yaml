services:
  fleet-api:
    environment:
      AUTH_CLIENT_EXPIRATION_PERIOD: "24h"
      AUTH_MINER_EXPIRATION_PERIOD: "24h"
      DB_NAME: "fleet"
      DB_ADDRESS: "timescaledb:5432"
      DB_USERNAME: "fleet"
      DB_PASSWORD: "fleet"
      DB_SSL_MODE: "disable"
      HTTP_LISTEN_ADDRESS: "0.0.0.0:4000"
      LOG_LEVEL: "INFO"
      FLEET_COMMAND_MAX_WORKERS: "500"
      FLEET_COMMAND_MASTER_POLLING_INTERVAL: "1s"
      FLEET_COMMAND_WORKER_EXECUTION_TIMEOUT: "30s"
      FLEET_COMMAND_BATCH_STATUS_UPDATE_POLLING_INTERVAL: "1s"
      FLEET_QUEUE_DEQUE_LIMIT: "500"
      FLEET_QUEUE_MAX_FAILURE_RETRIES: "5"
      # HTTP_PPROF_ADDR: "0.0.0.0:6060"  # Uncomment to enable pprof debug server; port mapping below restricts access to host loopback only
    # ports:
    #   - "127.0.0.1:6060:6060"  # pprof debug server (requires HTTP_PPROF_ADDR=0.0.0.0:6060)
    cap_add:
      - NET_ADMIN
      - NET_RAW

  # TimescaleDB (PostgreSQL with time-series extension)
  # Custom lightweight image (~123 MB) replacing timescale/timescaledb-ha (~1.67 GB)
  # Includes PostgreSQL 18, TimescaleDB, and timescaledb_toolkit — without HA bloat
  timescaledb:
    restart: always
    environment:
      POSTGRES_DB: "fleet"
      POSTGRES_USER: "fleet"
      POSTGRES_PASSWORD: "fleet"
    ports:
      - "5432:5432"
    command:
      - "postgres"
      - "-c"
      - "max_connections=300"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=768MB"
      - "-c"
      - "maintenance_work_mem=64MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
      - "-c"
      - "work_mem=32MB"
      - "-c"
      - "min_wal_size=1GB"
      - "-c"
      - "max_wal_size=4GB"
      - "-c"
      - "max_worker_processes=8"
      - "-c"
      - "max_parallel_workers_per_gather=4"
      - "-c"
      - "max_parallel_workers=8"
      - "-c"
      - "max_parallel_maintenance_workers=4"
      - "-c"
      - "timescaledb.max_background_workers=8"
    volumes:
      - timescaledb-data:/home/postgres/pgdata/data
    healthcheck:
      test:
        ["CMD-SHELL", "psql -U fleet -d fleet -c 'SELECT 1' > /dev/null 2>&1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    networks:
      - fleet-network

  # Stratum V2 translation proxy (bundled SRI tProxy).
  #
  # Profile-gated so the default `docker compose up` stays unchanged for
  # operators that don't run SV2. Enable with either:
  #   COMPOSE_PROFILES=sv2 docker compose up
  #   docker compose --profile sv2 up
  #
  # The config file at deployment-files/sv2/tproxy.toml is rendered by
  # install.sh from operator input (upstream sv2+* URL, miner-facing
  # stratum+* URL). Changing the upstream requires a container restart —
  # SRI does not hot-reload. See docs/stratum-v2-plan.md "Known
  # limitations" for the v1 scope boundaries (one upstream pool per
  # instance, no multi-site, no runtime reconfig).
  sv2-tproxy:
    # KNOWN LIMITATION: tag-pinned, not digest-pinned. The translator
    # sits in the miner→pool path and could rewrite mining traffic if
    # the upstream image ever served compromised bytes. v1 follow-up:
    # add a sha256 digest (`:tag@sha256:...`) once the supply-chain
    # verification flow is wired into the release artifacts. See
    # docs/stratum-v2-plan.md "Known limitations" §3.
    image: ghcr.io/stratum-mining/translator:1.5.1
    pull_policy: if_not_present
    restart: unless-stopped
    profiles: ["sv2"]
    # Host port and the in-container listener port both come from
    # STRATUM_V2_PROXY_DOWNSTREAM_PORT so a custom miner-facing port set
    # in install.sh (parsed from STRATUM_V2_PROXY_MINER_URL) is actually
    # reachable from miners. The tproxy.toml's downstream_port must
    # match — the installer rewrites both. Default 34255.
    #
    # STRATUM_V2_PROXY_DOWNSTREAM_HOST scopes the published listener to
    # a specific host interface; default 0.0.0.0 (all interfaces) keeps
    # the existing behavior. Operators on multi-homed or internet-facing
    # hosts can set this to a private/LAN IP literal in .env so the
    # unauthenticated stratum listener isn't exposed to the wider net.
    ports:
      - "${STRATUM_V2_PROXY_DOWNSTREAM_HOST:-0.0.0.0}:${STRATUM_V2_PROXY_DOWNSTREAM_PORT:-34255}:${STRATUM_V2_PROXY_DOWNSTREAM_PORT:-34255}"
    volumes:
      - ../sv2/tproxy.toml:/etc/tproxy/config.toml:ro
    environment:
      - RUST_LOG=info
    command:
      - "-c"
      - "/etc/tproxy/config.toml"
    healthcheck:
      # Probe the configured downstream port so a non-default miner-facing
      # port doesn't leave the healthcheck pinned to a stale 34255.
      test: ["CMD", "sh", "-c", "nc -z localhost ${STRATUM_V2_PROXY_DOWNSTREAM_PORT:-34255}"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  timescaledb-data:

networks:
  fleet-network:
    driver: bridge

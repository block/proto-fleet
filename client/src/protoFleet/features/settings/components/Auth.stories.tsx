import AuthenticationSettings from "./Auth";

export default {
  title: "Proto Fleet/Settings/AuthenticationSettings",
  component: AuthenticationSettings,
};

export const Default = () => <AuthenticationSettings />;

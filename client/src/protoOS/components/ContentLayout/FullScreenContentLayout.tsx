import { ContentLayoutProps } from "@/protoOS/components/ContentLayout/types";

const FullScreenContentLayout = ({ children }: ContentLayoutProps) => {
  return <div>{children}</div>;
};

export default FullScreenContentLayout;
